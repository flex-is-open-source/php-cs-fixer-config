stages:
    - build
    - test

composer:
    stage: build
    tags: [build-php]
    artifacts:
        paths:
            - var
            - vendor
    script:
        - composer install

code-coverage:
    stage: build
    tags: [build-php]
    needs:
        - composer
    script:
        - make coverage-cicd
    coverage: /^\s*Lines:\s*\d+.\d+\%/

analyse:
    stage: test
    tags: [test-php]
    needs:
        - composer
    dependencies:
        - composer
    script:
        - make cs-cicd
        - make analyse-cicd

unit-test:
    stage: test
    tags: [test-php]
    needs:
        - composer
    dependencies:
        - composer
    script:
        - make test-cicd
